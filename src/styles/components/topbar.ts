import styled from 'styled-components'

export const Navabar = styled.nav`
  width: 100%;

  display: flex;
  align-items: center;
  justify-content: space-between;

  position: absolute;
  top: 0;

  /* padding: clamp(1rem, 2vw, 2rem); */
`
